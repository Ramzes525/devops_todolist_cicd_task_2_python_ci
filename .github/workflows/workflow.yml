name: Python CI for GitHub Actions

run-name: ${{ github.actor }} - ${{ github.sha }}

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

env:
  PYTHON_VERSION: 3.8

jobs:
  python-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src

    steps:
      - name: Repository
        uses: actions/checkout@v4

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: '${{ env.PYTHON_VERSION }}'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install coverage flake8 pytest pytest-cov
          pip install -r requirements.txt

      - name: Check Code Style
        run: |
          flake8 src --statistics --exit-zero

      - name: Coverage report
        run: pytest --cov-report=xml --cov=.

      - name: Run Unit Test with pytest
        run: |
          pytest src/test_mate_app.py -v --cov-report=term-missing --cov=.

      - name: Check Code Complexity
        run: |
          flake8 src --statistics --max-complexity=10 --exit-zero

      - name: Upload Python Code as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: source-code
          path: .
